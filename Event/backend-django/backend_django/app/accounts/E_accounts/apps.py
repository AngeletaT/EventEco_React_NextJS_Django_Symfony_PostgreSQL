from django.apps import AppConfig

class EAccountsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'backend_django.app.accounts.E_accounts'
    label = 'e_accounts'  
    verbose_name = 'E Accounts'
